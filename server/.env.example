# Database
DATABASE_URL="file:./dev.db"

# Server
PORT=4000

# Scheduler
# Set to "true" to disable automatic periodic captures
DISABLE_SCHEDULER=false
# How often (minutes) the worker checks which apps are due; default 1 keeps short intervals accurate
SCHEDULER_CHECK_INTERVAL_MINUTES=1
